# MCP Production Environment Configuration
# IMPORTANT: Never commit this file with actual values to version control
# Use a secure secret management system for production deployments

# ======================
# Core Platform Settings
# ======================

# Production environment
NODE_ENV=production

# Production API endpoint
MCP_API_URL=https://api.mcp-platform.com
MCP_API_KEY=# Generate secure API key for production
CI_MCP_KEY=# Generate secure CI API key for production

# Platform data directory
MCP_HOME=/var/lib/mcp-platform

# ======================
# Database Configuration
# ======================

# Production PostgreSQL settings
DB_HOST=# Production database host (e.g., RDS endpoint)
DB_PORT=5432
DB_NAME=mcp_production
DB_USER=mcp_prod_user
DB_PASSWORD=# Use strong password from secret manager

# ======================
# Security & Authentication
# ======================

# JWT secret - MUST be unique for production
# Generate with: openssl rand -hex 64
JWT_SECRET=# Production JWT secret

# Admin credentials - Use strong, unique values
ADMIN_PASSWORD=# Strong admin password from secret manager
ADMIN_API_KEY=# Generate secure admin API key

# Encryption keys - MUST be unique and securely stored
# Generate with: openssl rand -hex 32
MCP_CONFIG_ENCRYPTION_KEY=# Production encryption key
ENCRYPTION_KEY=# Production tenant encryption key

# SSL/TLS Configuration
LETSENCRYPT_EMAIL=ops@your-domain.com

# ======================
# Service Configuration
# ======================

# Production ports (typically behind load balancer)
PORT=443
HEALTH_PORT=3999
HEALTH_SERVICE_URL=https://health.mcp-platform.com/health

# ======================
# External Service APIs
# ======================

# Production API keys (store in secret manager)
WEATHER_API_KEY=# Production weather API key
WEATHER_API_URL=https://api.openweathermap.org/data/2.5
CACHE_TTL=3600

# ======================
# Cloud Provider Settings
# ======================

# AWS Production Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=# Production AWS account ID
# Use IAM roles instead of keys when possible
# AWS_ACCESS_KEY_ID=# Only if IAM roles not available
# AWS_SECRET_ACCESS_KEY=# Only if IAM roles not available

# Azure Production Configuration
AZURE_SUBSCRIPTION_ID=# Production subscription ID
AZURE_LOCATION=eastus
AZURE_RESOURCE_GROUP=mcp-production

# GCP Production Configuration
GCP_PROJECT_ID=# Production project ID
GCP_REGION=us-central1

# ======================
# Container & Orchestration
# ======================

# Production container registry
DOCKER_REGISTRY=your-registry.azurecr.io

# Orchestration platform
ORCHESTRATOR=kubernetes

# Service mesh endpoint
CONSUL_HTTP_ADDR=http://consul.service.consul:8500

# ======================
# Monitoring & Logging
# ======================

# Production logging
LOG_LEVEL=warn

# Health check configuration
CHECK_INTERVAL=60000
HEALTH_TIMEOUT=10000

# Application version (updated during deployment)
APP_VERSION=# Set by CI/CD pipeline

# ======================
# Performance & Scaling
# ======================

# Rate limiting
RATE_LIMIT_WINDOW=60000
RATE_LIMIT_MAX=100

# Cache settings
ENABLE_CACHE=true
CACHE_MAX_SIZE=1000000

# ======================
# Backup & Recovery
# ======================

# Backup configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE="0 2 * * *"
BACKUP_RETENTION_DAYS=30

# S3 backup bucket
BACKUP_S3_BUCKET=mcp-platform-backups

# ======================
# Feature Flags
# ======================

# Production feature toggles
ENABLE_DEBUG_MODE=false
ENABLE_HOT_RELOAD=false
ENABLE_STRICT_VALIDATION=true
ENABLE_RATE_LIMITING=true
ENABLE_AUTH=true