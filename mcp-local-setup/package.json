{
  "name": "mcp-local-setup",
  "version": "1.0.0",
  "description": "MCP Local Setup - Testing Infrastructure",
  "private": true,
  "scripts": {
    "test": "jest",
    "test:unit": "jest tests/unit --testTimeout=10000",
    "test:integration": "jest tests/integration --testTimeout=30000",
    "test:e2e": "jest tests/e2e --testTimeout=60000",
    "test:all": "jest --testTimeout=60000",
    "test:coverage": "jest --coverage --testTimeout=60000",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "chalk": "^4.1.2",
    "cli-table3": "^0.6.3",
    "commander": "^11.0.0",
    "inquirer": "^8.2.6",
    "js-yaml": "^4.1.0",
    "ora": "^5.4.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}